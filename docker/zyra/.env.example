# Compose environment overrides for the zyra CLI image

# Tag to pull from GHCR (e.g., v1.2.3, latest)
ZYRA_TAG=latest

# Logging level for Zyra commands
LOG_LEVEL=info

# Container timezone (optional)
TZ=UTC

# Optional: set user mapping for Linux bind mounts
# UID=1000
# GID=1000

