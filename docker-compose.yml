services:
  datavizhub:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    command: ["sleep", "infinity"]