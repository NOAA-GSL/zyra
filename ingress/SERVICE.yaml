# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata:
  name: zyra-api
  namespace: zyra
  labels:
    app: zyra-api
spec:
  selector:
    app: zyra-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
